# HEIF fixtures

`opaque.heic` is the 64×64 generated HEVC still-image fixture from the
`heif-oxide` 0.1.0 test suite. It is distributed under that package's
MIT OR Apache-2.0 terms and contains no user metadata.
