# Issues #332, #350, #383, #394, and #397 Batch Implementation Plan

> For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** Complete the five desktop issues in one origin/main-based branch and one verified pull request.

**Architecture:** Preserve Rust-owned state and existing Tauri/state-delta transport. Reuse the existing event cache, Focused timeline, thread, and Composer primitives. Each issue gets a focused red-green verification slice before the next slice is started.

**Tech Stack:** Rust workspace, Tauri, React/TypeScript, Vitest, Playwright, ImageMagick, the checked-in Python ICNS helper, and local Conduit/Tuwunel QA.

---

## File map

- Search: apps/desktop/src/domain/types.ts, Shell.tsx, App.tsx, browserFakeApi.ts, Tauri dto.rs and commands/search.rs, koushi-state search.rs and reducer/search.rs, koushi-core command.rs and search.rs, koushi-sdk lib.rs.
- Icon: assets/branding/koushi-photon-macos.svg, scripts/generate-koushi-icons.sh, scripts/generate-koushi-icons.test.mjs, apps/desktop/src-tauri/icons.
- Scheduled editor: apps/desktop/src/components/composer.tsx, mediaLists.tsx, composer.test.tsx, mediaLists.test.tsx, koushi-state timeline/reducer files, koushi-core command/account/runtime files, and Tauri commands.
- Threads: koushi-state thread state/action/effect/reducer files, koushi-core threads_list/account/event files, Tauri DTO, domain coreEvents/types, ThreadsListView, App, Shell, and rightPanel.
- Pinned navigation: koushi-state room_interactions/reducer/room, koushi-sdk lib.rs, koushi-core room/command/runtime, Tauri navigation, domain rightPanel, mediaLists, panes, rightPanel, TimelineView, and App.

## Task 1: Current-conversation search (#397)

- [ ] Write failing Rust tests in crates/koushi-state/tests/search_state.rs for a normal room, a DM, and a missing active room. The missing-room case must assert that resolution is not AllRooms.
- [ ] Run cargo test -p koushi-state --test search_state current_conversation and confirm the failure is caused by the CurrentRoom/Dms contract and AllRooms fallback.
- [ ] Write failing Shell/App tests asserting that the selector has Room/DM, has no separate DMs option, and App initializes currentConversation.
- [ ] Rename the state/core/Tauri scope to CurrentConversation, remove the all-DMs variant, resolve both room kinds through the active room ID, and return an empty scoped result when no room is active.
- [ ] Update browser fake filtering, i18n, Tauri DTO mirrors, CoreEvent fixtures, and search command traces together.
- [ ] Run cargo test -p koushi-state --test search_state current_conversation, npm --prefix apps/desktop test -- src/components/Shell.test.tsx src/domain/desktopModel.test.ts, and npm --prefix apps/desktop run typecheck. Confirm all pass.
- [ ] Commit with git commit -m "feat: make Room/DM the default search scope".

## Task 2: Full-bleed macOS icon (#383)

- [ ] Add a failing scripts/generate-koushi-icons.test.mjs test that requires a macOS-specific source, an opaque full-bleed background, and explicit macOS ICNS input selection.
- [ ] Run node --test scripts/generate-koushi-icons.test.mjs and confirm it fails on the current shared source.
- [ ] Create assets/branding/koushi-photon-macos.svg with a 128 by 128 opaque background and the existing centered photon artwork without pre-rounded transparent corners.
- [ ] Modify scripts/generate-koushi-icons.sh so PNG/ICO/favicon outputs retain the existing source and only ICNS inputs use the macOS source.
- [ ] Run bash scripts/generate-koushi-icons.sh, the generator test, file apps/desktop/src-tauri/icons/icon.icns, and git diff --check. Confirm cross-platform generated assets are unchanged.
- [ ] Commit with git commit -m "fix: use a full-bleed macOS app icon source".

## Task 3: Scheduled-message body editing (#350)

- [ ] Add a failing ScheduledSendStore test proving body and send_at_ms update while room, thread root, scheduled ID, and handle remain stable.
- [ ] Add a failing runtime test proving a server delayed event is cancelled before a replacement with the updated body and time is created.
- [ ] Run cargo test -p koushi-state --test scheduled_send_state update_scheduled and cargo test -p koushi-core --test runtime_scheduled_send update_scheduled; confirm the missing command/store behavior.
- [ ] Add a controlled shared editor component in composer.tsx containing the IME-owned textarea, formatting toolbar, emoji picker, mention autocomplete, and text-range helpers. Keep the normal Composer footer in its existing wrapper.
- [ ] Render that shared editor from ScheduledMessagesList with body and datetime-local controls. Save must pass both values; Cancel must leave the item unchanged.
- [ ] Add UpdateScheduledSend with scheduled_id, body, and send_at_ms through command, reducer action, account/runtime server cancel-recreate, local store update, Tauri command, browser fake, and persistence projection.
- [ ] Add failing/green component tests for bold, italic, link, list, code, emoji, mention, body value, and combined Save payload.
- [ ] Run npm --prefix apps/desktop test -- src/components/composer.test.tsx src/components/mediaLists.test.tsx, the two scheduled Rust tests, node --test scripts/check-ime-text-inputs.test.mjs, node scripts/check-ime-text-inputs.mjs, and the ImeTextControl test.
- [ ] Commit with git commit -m "feat: edit scheduled message bodies with shared composer controls".

## Task 4: Home and Space thread aggregation (#332)

- [ ] Add failing pure tests for Home and Space scope membership, deduplication by room ID plus root event ID, latest-reply ordering, and row room ownership.
- [ ] Run cargo test -p koushi-state --test thread_state aggregate_threads and cargo test -p koushi-core --lib threads_list aggregate; confirm the current room-only implementation fails.
- [ ] Add a Rust-owned ThreadsListScope with Room, Home, and Space variants; carry it through actions, effects, events, Tauri DTO, TypeScript types, and existing room entry points.
- [ ] Add room_id to ThreadsListItem and preserve current room rows and thread-open behavior.
- [ ] Extend the core actor to open one bounded SDK ThreadListService per room in the requested scope, merge/deduplicate/sort rows, and paginate each participating service once per explicit load-more request.
- [ ] Resolve Home and Space room sets in Rust before subscription. An unreliable Space membership set must yield an unavailable/empty scope instead of global data.
- [ ] Add Home and selected-Space entry points to the existing right-panel navigation. ThreadsListView must use each row room_id and must not derive aggregate rows from React room/timeline data.
- [ ] Run the aggregate Rust tests, npm --prefix apps/desktop test -- src/components/ThreadsListView.test.tsx src/App.test.tsx, and npm --prefix apps/desktop run typecheck.
- [ ] Commit with git commit -m "feat: aggregate threads for Home and spaces".

## Task 5: Pinned-message panel and event-ID navigation (#394)

- [ ] Add failing tests for ordered pinned rows, duplicate suppression, the pinned right-panel mode, compact entry-point activation, and callbacks carrying the owning room and exact event ID.
- [ ] Add a reducer test proving a failed target load keeps the pinned panel available for retry, then run the focused state/UI tests to observe the current passive-list failure.
- [ ] Add pinned to RightPanelMode and RightPanelIntent, replace the passive timeline block with a compact accessible entry button, and render a room-scoped pinned panel.
- [ ] Enrich the Rust pinned projection with the preview/state/relation data required by the panel while keeping m.room.pinned_events order authoritative.
- [ ] Add a typed event-target command. Core must call load_or_fetch_event, perform targeted room repair before the existing Focused/anchored timeline projection, and fail without jumping to latest.
- [ ] Keep pinned mode open while the center timeline changes and highlight the target by event ID. Root events use the main focused timeline; thread replies select the owning room, open the root thread, and focus the reply.
- [ ] Run cargo test -p koushi-state --test room_interactions pinned_panel, cargo test -p koushi-core --lib focused event, npm --prefix apps/desktop test -- src/domain/rightPanel.test.ts src/components/mediaLists.test.tsx src/App.test.tsx src/components/TimelineView.test.tsx, and npm --prefix apps/desktop run typecheck.
- [ ] Commit with git commit -m "feat: add pinned message panel navigation".

## Task 6: Integrated verification, PR, and merge

- [ ] Regenerate the frontend golden and CoreEvent contract artifacts using the repository-required commands. Update Tauri DTOs, TypeScript types, browser fakes, harness snapshots, and generated JSON together.
- [ ] Run node --test scripts/check-ime-text-inputs.test.mjs, node scripts/check-ime-text-inputs.mjs, npm --prefix apps/desktop run lint, npm --prefix apps/desktop run typecheck, focused koushi-state/core tests, the SDK path guard, and git diff --check. Capture each exit status independently in a log.
- [ ] Run the local headless scenarios for Room/DM search default, scheduled body update, pinned event-ID navigation, and Home/Space thread aggregation. Assert CoreEvent/AppState/DOM tokens, not logs or fixed sleeps.
- [ ] Fetch origin, read git diff origin/main...HEAD and git status --short, and review every acceptance criterion for all five issues. Include untracked files in the review.
- [ ] Push the batch branch and open exactly one PR with one summary, verification evidence, and Closes entries only for fully evidenced issues.
- [ ] Merge with the repository-approved non-squash method and verify the PR is merged and all five GitHub issues are closed.

