# Test Report: koushi-release

**Date:** 2026-08-18 10:55:34 Europe/Vienna
**Persona:** Mika — Git-comfortable maintainer new to Koushi release automation
**Profile:** ephemeral
**Use Case:** Prepare version 0.4.3 locally without any GitHub mutation
**Expected Outcome:** A reviewable local version change set with explicit checks and no signing-key access
**Phases tested:** skill discovery, mode selection, version authorization, preparation routing, external-action boundary
**Decision points exercised:** 1 of 1
**Verdict:** pass
**Critical Issues:** 0

## Flow Completeness

- **Phases reached:** Explain/prepare routing, explicit target version, canonical runbook loading, local-check selection, and external-action authorization.
- **Phases skipped:** Actual manifest mutation and release execution were intentionally skipped because this forward test was read-only and must not alter the live project or GitHub state.
- **Decision points exercised:** The persona selected exact SemVer `0.4.3` and authorized local preparation only.
- **Untested branches:** Monitoring a live workflow and troubleshooting a failed draft publication.

## Interaction Trace

1. The skill required the canonical runbook to be read before action and classified the request as preparation.
2. The simulated maintainer supplied `0.4.3` and explicitly denied push, PR, merge, tag, release, upload, signing, and keychain actions.
3. The skill's boundaries preserved that authorization and routed preparation to the three synchronized manifests and local gates only.
4. A possible ambiguity about whether preparation includes signing was found. The runbook was clarified to state that local preparation does not build installers, access signing credentials, or modify the keychain.

## Output Validation

- **Expected files:** In a live authorized run, the three version manifests would change together.
- **Actually created:** No release fixture or manifest change; only this test report was created.
- **Format check:** Both project skill entry points pass `quick_validate.py`; the common, Claude Code, and OpenCode skill bodies are byte-identical. The repository contract test verifies all four invocation forms and the three manifest paths.

## Broken References

None. The canonical runbook, workflow, three manifests, shared skill, Claude Code skill, and OpenCode skill all exist. Pi and Codex use the shared `.agents/skills` entry point.

## Persona Interview

1. The most useful part was an explicit local checklist with verification commands.
2. The potentially confusing point was whether “prepare” included signing or tagging.
3. The flow was appropriately short for an experienced Git user.
4. The persona wanted a hard stop before signing credentials or keychain changes; the skill provides one.
5. The expected takeaway was a locally verified patch-version diff ready for inspection.
6. The persona requested no additional external action.

## Expected vs Actual Outcome

The tested control flow matched the expected outcome: it required an exact version, identified the intended local-only scope, selected the correct manifests and gates, and stopped before external or credential-bearing actions. The read-only forward test did not create the actual version diff, by design.

## Structural Observations (from test executor)

- **Discovery intent vs experience:** The skill is intended to be callable from four agents. Codex and Pi share `.agents/skills`; Claude Code and OpenCode have native discovery entries. OpenCode 1.17.11 found the native entry in `opencode debug skill`.
- **Routing intent vs experience:** The explain, prepare, monitor, and troubleshoot modes were distinct and preparation did not accidentally imply publication.
- **Authorization intent vs experience:** The persona's denial of GitHub and signing actions remained binding throughout the flow.
- **Documentation intent vs experience:** The canonical runbook prevented procedural duplication. The only observed ambiguity—whether preparation touches signing credentials—was corrected in the runbook.

## Suggestions

1. Exercise the monitor branch after the first real workflow run exists, using read-only `gh run` and `gh release view` commands.
2. Exercise failure recovery when a real draft-only failure is available; do not manufacture one against the live repository.

No temporary test files required cleanup.
