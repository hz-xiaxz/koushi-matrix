# Issue #755 Phase D checkpoint

Status: implemented and focused verification green; different-model integration checkpoint pending.

## Scope

Remove the adapter's second composer identity registry while preserving the approved Core lease lifecycle. Secure-backup Phase E remains untouched.

## Implementation

- `ComposerRendererGeneration` and `ComposerDraftLeaseId` expose canonical nonzero decimal `u64` parse/format methods with closed wire errors and private-safe Debug.
- Core connection admission rechecks the exact Ready `SessionKeyId`, active main/thread `ComposerTarget`, live renderer generation, lease scope, and permit kind before lease/terminal permission.
- Deleted `ComposerDraftTransportIdentities`, adapter counters/maps/mutex, install/look-up translations, and source-only mirror tests from `CoreRuntimeState` and Tauri.
- Tauri begin/acquire/release and terminal handlers parse or format opaque strings only; parsing grants no authority. The existing second-release mismatch policy remains explicit.
- Browser fake and Playwright harness identities now use canonical numeric strings without a compatibility shim.
- This ownership change adds no AppState/AppAction/reducer transition.

## Verification

- RED: Core/Tauri compilation failed before the wire/admission API and adapter deletion were complete.
- `cargo test -p koushi-core --test composer_draft_wire`: 4 passed.
- `cargo test -p koushi-core --test composer_draft_lifecycle`: 7 passed.
- `cargo test -p koushi-desktop`: 110 library tests and 5 integration tests passed.
- Frontend TypeScript typecheck passed using the shared dependency tree; the temporary worktree symlink was removed.
- `playwright test e2e/app-harness-resource-lifecycle.spec.ts`: 11 passed; the temporary worktree dependency symlink was removed.
- `cargo test -p koushi-core --lib`: 936 passed, 8 ignored.
- Strict structure checker and its 19 tests, agents docs, rustfmt, and diff checks passed.

## Integration checkpoint

- Reviewer: `deepseek-brainstormer`, `VERDICT: CORRECT-TO-CONTINUE`; wire canonicality/privacy, Core-only authority and active scope admission, retirement/forgery/release/exhaustion behavior, adapter mirror deletion, test-backend parity, tests, and canon were verified.
- This is an additional checkpoint under the approved #755 design.
