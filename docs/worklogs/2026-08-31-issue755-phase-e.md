# Issue #755 Phase E checkpoint

Status: implemented and focused verification green; different-model integration checkpoint pending.

## Scope

Move secure-backup setup/re-enable confirmation admission to Rust and leave only accessible dialog/input presentation in React. This completes the planned #755 implementation phases.

## Implementation

- Added closed `SecureBackupSetupIntent::{InitialSetup, Reenable { confirmed }}` to both projected `SecureBackupSetupRequested` action and `SecureBackupSetupRequest`.
- `AppActor` now projection-gates `BootstrapSecureBackup` before `AccountActor` routing. Gate-incompatible, unconfirmed, duplicate, stale, and forged intents emit typed private-safe confirmation-required/failed-no-op failures with no actor effect.
- Only confirmed re-enable at `ExplicitlyDisabledRequiresSetup` maps to the SDK re-enable call; initial setup maps to ordinary setup. The SDK's fresh inspection/confirmation guard remains authoritative.
- Deleted Tauri's native bilingual warning dialog, boolean translation, separate re-enable command, and registration. The remaining command transports the typed intent only.
- React renders the existing catalog-backed accessible confirmation dialog. Cancel sends no command; confirm sends the exact typed intent. Dialog authority, selected destination, and outstanding chooser epoch reset on gate or account-owner change so stale UI confirmation/path results cannot survive.
- Browser fake and Playwright harness mirror the transport enum without owning admission semantics. No secret enters projected state, errors, logs, or tests.
- Updated the structural E2EE command checker to require typed transport and reject any native re-enable route/dialog copy.

## Verification

- RED: focused state/Core/Tauri/frontend compilation and tests failed before all typed action/request/UI call sites were migrated.
- `cargo test -p koushi-state --test secure_backup_setup_admission`: 3 passed.
- Core secure-backup admission unit filter: 2 passed.
- `cargo test -p koushi-desktop`: 110 library tests and 5 integration tests passed.
- Frontend TypeScript typecheck passed using the shared dependency tree; temporary worktree symlink removed.
- Focused Vitest (`SessionVerificationGate`, browser fake, Tauri client): 3 files and 212 tests passed; the gate test proves visible dialog, cancel/no-command, stale gate reset, and exact confirmed intent.
- `cargo test -p koushi-state --lib`: 40 passed.
- `cargo test -p koushi-core --lib --test command_redaction`: 938 library tests passed, 8 ignored; 15 redaction tests passed.
- Frontend lint and production build passed; generated `dist`/`.vite` artifacts and temporary dependency symlinks were removed.
- `playwright test e2e/security-e2ee.spec.ts`: 6 passed, including exact initial-setup typed IPC.
- Strict structure checker and its 19 tests, agents docs, rustfmt, and diff checks passed.

## Integration checkpoint

- Round 1 reviewer: `deepseek-brainstormer`, `VERDICT: CORRECT-TO-CONTINUE`; typed routing/admission, SDK authority, adapter deletion, accessible UI/transport, tests, and canon were verified. Its non-blocking stale destination/chooser observation was fixed with an owner/gate epoch and focused regression proof.
- Round 2 reviewer: `deepseek-brainstormer`, `VERDICT: CORRECT-TO-CONTINUE`; owner/gate reset, path clearing, chooser epoch fencing, cancel/no-command, exact confirm regression proof, and unchanged Rust admission were verified.
- This is the final implementation checkpoint under the approved #755 design; an exact full-diff verdict still follows.
